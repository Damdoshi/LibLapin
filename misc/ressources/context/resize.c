#include		"program.h"
#include		"CONTEXT.h"

t_bunny_response	CONTEXT_resize(const t_bunny_window		*win,
				       const t_bunny_position		*siz,
				       t_CONTEXT			*CONTEXT)
{
  (void)win;
  (void)siz;
  (void)CONTEXT;
  return (GO_ON);
}
