
string lol = "this_is_lol"
int zob = 1

If lol == "this_is_lol"
   real rel = 4.2
   "Inside"
   If 0 < rel < 42
      If zob
        "Very deep! Here thanks to the help of lol: '", lol, "' zob: '", zob, "' and rel: '", rel, "'!"
      EndIf
      "After deep"
   EndIf
Else If true
  "Else If"
Else
  "Else"
EndIf

